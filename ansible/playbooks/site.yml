---
- name: Aplicar configuración completa del servidor
  hosts: all
  become: true
  roles:
    - role: common
    - role: security
    - role: docker
    - role: traefik_portainer
  # El role 'update' se ejecuta por separado con update.yml
  # para evitar actualizaciones de contenedores en cada re-apply:
  #   ansible-playbook ansible/playbooks/update.yml
