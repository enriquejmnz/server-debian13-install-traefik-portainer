---
- name: Actualizar contenedores gestionados
  hosts: all
  become: true
  roles:
    - role: common
    # El role update verifica Docker internamente; no es necesario re-ejecutar docker aquí
    - role: update
