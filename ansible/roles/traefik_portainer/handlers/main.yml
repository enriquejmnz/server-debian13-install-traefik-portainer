---
- name: Restart Traefik Portainer stack
  ansible.builtin.shell: |
    cd "{{ install_dir }}" && docker compose down && docker compose up -d
  args:
    executable: /bin/bash
  changed_when: true
